spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://departments-postgres-server-service/departments
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: create
